stages:
  - build

job_build:
  stage: build
  script:
    - # Your build commands here
    - # Generate JSON artifact
    - echo '{"key": "value", "another_key": "another_value"}' > artifacts/data.json
  artifacts:
    paths:
      - artifacts/
